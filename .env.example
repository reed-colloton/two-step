# The dev server reads this from the inherited global environment.
# If already exported in your shell, no local .env file is needed.
# Production: configure this as a secret in Sites.
OPENROUTER_API_KEY=
